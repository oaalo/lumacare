# تجربة LATYAI داخل GitHub

1. أنشئ Codespace من الفرع `codex/grounded-ai-chat` باستخدام **Code → Codespaces → New with options**.
2. إذا ظهر حقل `GROQ_API_KEY`، الصق مفتاح Groq فيه. سر Actions الذي أضفته سابقًا لا ينتقل تلقائيًا إلى Codespaces.
3. اختر جهازًا صغيرًا وتأكد من أن لديك حصة مجانية متبقية في حساب GitHub قبل الإنشاء؛ لا تفعّل الفوترة أو تضف وسيلة دفع لهذه التجربة.
4. بعد تجهيز البيئة، يشغّل المشروع الاختبارات ويبدأ الخادم تلقائيًا. افتح **Ports → 3000 → Open in Browser** إذا لم يُفتح الموقع.
5. أبقِ المنفذ **Private**. البريد في واجهة LATYAI مدخل تجريبي، وليس مصادقة وزارة الصحة.
6. جرّب السؤال: «المريض ما يبي يلبس السوار، وش أسوي؟» ثم «هل لازم أوثق؟». جرّب أيضًا سؤالًا عن موضوع غير موجود في السياسة وتأكد من الامتناع عن الإجابة.
7. عند الانتهاء اختر **Stop codespace** من قائمة Codespaces. إغلاق التبويب وحده ليس إيقافًا فوريًا للبيئة.

إذا لم تضف المفتاح أثناء الإنشاء، أضفه في إعدادات المستودع: **Secrets and variables → Codespaces → New repository secret** بالاسم `GROQ_API_KEY`، ثم أوقف البيئة وشغّلها مجددًا.

يمكن تشغيل الخادم يدويًا بالأمر `npm start`. لا تضع المفتاح في ملفات الكود أو ترسله في المحادثة. الخادم يستخدم Groq ولا يحاول التحول إلى مزود مدفوع.

هذه نسخة تجربة من نفس المستودع؛ لم تُنشر على Netlify، ولم تُختبر بعد بإجابات فعلية من حسابك.
