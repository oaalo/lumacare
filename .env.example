# Server only. Never put the real key in app.js or commit it.
OPENAI_API_KEY=
# Choose a model available to your account that supports Responses + Structured Outputs.
OPENAI_MODEL=
PORT=3000
LUMACARE_ORIGIN=http://127.0.0.1:3000
